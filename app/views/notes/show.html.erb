<%= t('Show') %>

<div class="row-fluid">
	<div class="span12">
		<div class="padding20">
			<p class="lead"> <%= @note.title %> </p>

			<div class="top20">
				<%= @note.content %>
			</div>
		</div>
	</div>
</div>
